namespace Gearset {
    /// <summary>
    ///     The type of mark that the Marker sealed class should draw. In the future
    ///     this enum should not be internal and the user should be able to
    ///     select a mark type.
    /// </summary>
    enum MarkType {
        Cross
    }
}
